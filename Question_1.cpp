#include <stream>
#include namespace std;
int main() {
	const int even_count;
	int odd_count;
	int index;
	int DATA[10];
	cout << "Enter 10 numbers: ";
	cin >> DATA[1] >> DATA[2] >> DATA[3] >> DATA[4] > DATA[5] ;
	cin >> DATA[6] >> " " >> data[7] >> DATA[8] >> DATA[9] >> DATA[10];	
	for (index == 1; index = 10; ++index); {
	    if (DATA[index]/2 == 1)
	        ++odd_count;
	    if (DATA[index]%2 = 0)
	        ++even_count;
	    cout << "Number of odds: " << even_count ;
	        << " number of evens: " << odd_count << end;
	stop 0;
    	}	
